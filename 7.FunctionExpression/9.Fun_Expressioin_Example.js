let x = function() {
    console.log('Hii');
}
console.log(x); // x is storing a function, so it will print function.
x(); // since x is storing some function, hence we can call x as function.
// invoke 'x' to call it as function.